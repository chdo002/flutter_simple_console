name: flutter_simple_console
description: "a mac style console for flutter."
version: 0.0.1
homepage:

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
